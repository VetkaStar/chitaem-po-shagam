# Карта модулей

Сначала выбирайте строку таблицы по задаче. Читать всё приложение для локальной правки не нужно.

| Задача | Файл | Ответственность |
| --- | --- | --- |
| Точка входа | app/page.tsx | Только подключение ReadingApp |
| Компоновка страницы | features/lesson/ReadingApp.tsx | Сборка интерфейса из компонентов |
| Состояние занятия | features/lesson/use-lesson.ts | Переходы, счёт, сохранение, координация микрофона и озвучки |
| Типы и настройки по умолчанию | features/lesson/config.ts | Stage, Mode, Settings, Entry, названия букв |
| Карточка задания | features/lesson/ExerciseCard.tsx | Чтение, ввод, визуальная обратная связь, помощь |
| Меню разделов и режимов | features/lesson/ {LessonSidebar,LessonToolbar,LessonHeader}.tsx | Навигация и вход в настройки |
| Настройки взрослого | features/lesson/ParentSettings.tsx | Настройки, выбор микрофона, источники, экспорт |
| Диалоги | features/lesson/RestDialog.tsx, MicrophoneConsent.tsx | Отдых и согласие |
| Звук и распознаватель | lib/local-speech.ts | Загрузка Vosk, поток микрофона, спектр, события звука и текста |
| Медленное чтение | lib/slow-reading.ts | Сбор последовательных частей, растянутые гласные, срок попытки |
| Проверка речи | lib/feedback.ts | Верный ответ, известная замена, посторонняя речь, отказ |
| Учебный материал | lib/learning.ts | Буквы, слоги, картинки, базовые проверки |
| Перемешивание и смысл ответа | lib/session.ts | Набор слов, случайная колода, синонимы, части и сходства |
| Опечатки | lib/typo.ts | Исправления и позиции букв |
| Картинки | components/picture-scene.tsx | Контекстные варианты изображения |
| Игры отдыха | components/rest-hub.tsx и соседние игровые компоненты | Выбор игры, правила и движение |
| Оформление | app/globals.css | Стили, эффекты, режим без движения |
| Проверки | tests/regression.mjs | Изолированные проверки доменной логики и потока |

## Связи

ReadingApp вызывает useLesson один раз. Компоненты получают типизированный Pick<LessonModel> только с нужными полями. Они не создают второй микрофон и не ведут отдельный счёт. Бизнес-правила находятся в lib; useLesson связывает их с состоянием интерфейса. Модель распознавания лежит в public/speech; её контрольные суммы проверяет загрузчик.

## Изоляция версий

Работать только в reading-app-v3. Ключ прогресса: reading-steps-v3. У исходных версий другие репозитории, адреса и ключи. Локальный node_modules — ссылка на установленные зависимости lab: не редактировать содержимое зависимостей и не обновлять их через эту ссылку. Для изменения зависимостей сначала нужна отдельная установка в v3.

## Правила изменений

1. Выбрать модуль, изменить только его контракт и необходимые места вызова.
2. Для изменения поведения обновить tests/regression.mjs и соответствующий раздел docs/READING.md.
3. Выполнить npm test, npm run typecheck и npm run build.
4. Для микрофона дополнительно проверить реальный звук, паузу, подсказку, следующий экран и остановку. Текстовые тесты не доказывают качество распознавания детской речи.
5. Добавить запись в CHANGELOG. Не переносить исправления в рабочие старые версии без отдельного указания.

Темы: features/lesson/TopicNavigation.tsx — детская навигация; lib/topics.ts — порядок тем; use-lesson.ts/changeTopic — сброс занятия при переходе.
